import React from 'react';
import Dropdown from './dropdown';

const ProductsDropdown = ({ subnavlist }) => (
    <Dropdown type={"Products"} subnavlist={subnavlist} />
);

export default ProductsDropdown;