import React from 'react';
import NavLinks from './navlinks';

const Navbar = () => {
    return (
        <header id="navbar">
            <image></image>
            <NavLinks />
        </header>
    );
}

export default Navbar;