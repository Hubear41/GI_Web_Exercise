import React from 'react';
import NavLinks from './navlinks';

const Navbar = () => {
    return (
        <header id="navbar">
            <i className="icon"></i>
            <NavLinks />
        </header>
    );
}

export default Navbar;