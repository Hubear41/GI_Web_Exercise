import React from 'react';
import NavLinks from './navlinks';

const Navbar = () => {
    return (
        <header id="navbar">
            <img></img>
            <NavLinks />
        </header>
    );
}

export default Navbar;